{
  "name": "@typographist/utils",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && cross-env rollup -c && yarn build:copy-files",
    "build:copy-files": "node ../../scripts/copy-files.js",
    "clean": "cross-env rimraf dist"
  }
}
